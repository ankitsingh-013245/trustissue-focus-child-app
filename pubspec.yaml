name: trustissue
description: A private, local-first focus and app-blocking tool.
publish_to: "none"
version: 0.2.4+11

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/aurora_bg.png
    - assets/images/panda_loader.png
